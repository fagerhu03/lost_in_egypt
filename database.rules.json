{
  "rules": {
    ".read": false,
    ".write": false,
    "guide_locations": {
      "$guideId": {
        ".read": "auth != null",
        ".write": "auth != null && auth.uid === $guideId"
      }
    }
  }
}
